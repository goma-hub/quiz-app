# quiz-app

一般常識をテーマにした、ブラウザで動作するクイズアプリです。

## 技術スタック

- HTML
- CSS
- JavaScript（フレームワーク・ライブラリに依存しない）

## 開発方針

- HTML は意味に沿った要素を使い、見出し・ボタン・フォームなどのアクセシビリティを保つ。
- CSS はレスポンシブ対応を前提とし、スマートフォンでも読みやすく操作しやすいレイアウトにする。
- JavaScript は役割ごとに関数を分け、状態管理と画面描画の責務を明確にする。
- クイズの問題、選択肢、正解、解説などのデータ構造は一貫させ、追加・修正しやすくする。
- 正解・不正解の表示、スコア、進捗、終了結果を利用者に分かりやすく示す。
- 外部依存を追加する前に、HTML/CSS/JavaScript だけで実現できるかを検討する。

## 実装時の注意

- 既存のファイル構成・命名規則・スタイルを優先し、不要な全面書き換えは避ける。
- ユーザーが操作する要素はキーボードでも操作できるようにする。
- 画面に表示する日本語は自然で簡潔な表現にする。
- 変更後はブラウザで主要な導線（開始、回答、次の問題、結果確認）を確認する。

## GitHubリポジトリ
https://github.com/goma-hub/quiz-app
